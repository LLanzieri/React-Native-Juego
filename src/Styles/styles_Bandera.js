import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    containerBandera: {
        /* borderColor: 'green',
        borderWidth: 2, */
        height: 150,
        width: 250,
        margin: 10,
    },

});
